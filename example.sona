// Simple Sona program to test the transpiler
let name = "Sona Transpiler"
let version = "0.7.1"

print("Welcome to " + name + " v" + version + "!")

func greet(person) {
    return "Hello, " + person + "!"
}

let message = greet("World")
print(message)

let numbers = [1, 2, 3, 4, 5]
for num in numbers {
    print("Number: " + str(num))
}
